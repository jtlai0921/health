<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jtlai0921.pass">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.*" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_2_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Main.index"></activity>
        <activity android:name=".Main.author" />
        <activity android:name=".Main.record" />
        <activity android:name=".Main.showdata" />
        <activity android:name=".Main.suger" />
        <activity android:name=".Main.pressure" />
        <activity android:name=".Main.graph" />
        <activity android:name=".Main.sign_up" />
        <activity android:name=".Main.bmi" />
        <activity android:name=".Main.first">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>